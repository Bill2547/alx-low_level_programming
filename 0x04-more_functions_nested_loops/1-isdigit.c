#include "main.h"

/**
 * _isdigit - checks for the upprecase character
 * @c: the character to be cheked
 * Return: 1if Cis uppercase and 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}

