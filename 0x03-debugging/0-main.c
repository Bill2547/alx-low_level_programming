#include "main.h"

/**
 * positive_or_negative - checks that the number is zero if given zero
 * @i: the number to be checked
 * Return: Always 0 if 0
 */

int main(void)
{
	int i;

	i = 0 ;
	
	positive_or_negative(i);

	return (0);
}
